// here we load the "dev" environment by default
// `npm run build` will call `webpack --config config/webpack.prod.js`
// and load the "dev" environment

export { environment } from './environment.dev';
