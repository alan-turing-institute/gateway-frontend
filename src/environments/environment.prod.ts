import { Environment } from './environment.interface';

export const environment: Environment = {
    target: 'prod',
    apiRoot: 'http://science-gateway-middleware-dev.azurewebsites.net/',
    production: true
}
